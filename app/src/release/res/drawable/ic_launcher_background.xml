<?xml version="1.0" encoding="utf-8"?>
<!-- The background layer of the adaptive icon: at the centre the red the
     iterating channel carries flat, out to black.

     The canvas is 108dp, so its corners sit sqrt(54^2 + 54^2) = 76.4dp from the
     centre. A radius of 76dp completes the ramp there, leaving no flat band
     wherever a launcher's mask cuts. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="radial"
        android:centerX="0.5"
        android:centerY="0.5"
        android:gradientRadius="76dp"
        android:startColor="#B71C1C"
        android:endColor="#000000" />
</shape>
